To search, upload a file that contains multiple documents. Each record has a `text` field with the document text. Currently only support JSONL files. For more details and examples, refer to the [OpenAI docs](https://beta.openai.com/docs/guides/searches).

Click on the searches file ID in the table to open the test UI. From there, you can do a search and see which documents match and their match score.